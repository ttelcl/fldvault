<UserControl
    x:Class="KeyLoader.Main.MasterVaults.EditingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:KeyLoader.Main.MasterVaults"
    xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    d:DataContext="{d:DesignInstance local:MasterVaultViewModel, IsDesignTimeCreatable=False}"
    mc:Ignorable="d" 
    d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border
            BorderBrush="{DynamicResource MahApps.Brushes.AccentBase}"
            BorderThickness="1"
            Grid.Row="0">
            <ItemsControl
                ItemsSource="{Binding Keys}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate >
                        <local:ViewingKeyView/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>
        <!-- edit buttons -->
        <Border
            MinHeight="25"
            BorderBrush="{DynamicResource MahApps.Brushes.AccentBase}"
            Background="{DynamicResource MahApps.Brushes.Accent3}"
            BorderThickness="1"
            Grid.Row="1">
            <StackPanel Orientation="Horizontal">
                <Button
                    Command="{Binding TryPasteCommand}">
                    <TextBlock>Paste Key Info</TextBlock>
                </Button>
            </StackPanel>
        </Border>
        <!-- footer -->
        <Border
            BorderBrush="{DynamicResource MahApps.Brushes.AccentBase}"
            Background="{DynamicResource MahApps.Brushes.Accent4}"
            BorderThickness="1"
            Grid.Row="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border
                    Grid.Column="1">
                    <Button
                        MinWidth="80"
                        Margin="5,5"
                        Command="{Binding Owner.SaveAndViewCommand}">
                        <TextBlock
                            TextAlignment="Center"
                            Text="{Binding Owner.SaveButtonLabel}"/>
                    </Button>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
