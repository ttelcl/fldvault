<UserControl
    x:Class="KeyLoader.Main.MessageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:KeyLoader.Main"
    xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    d:DataContext="{d:DesignInstance local:MainViewModel, IsDesignTimeCreatable=False}"
    DataContextChanged="MessageView_DataContextChanged"
    mc:Ignorable="d" 
    d:DesignHeight="450" d:DesignWidth="800" IsVisibleChanged="MessageView_IsVisibleChanged">
    <Border
        Background="#88000000">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="4*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border
                Grid.Row="1" Grid.Column="1"
                BorderThickness="1"
                BorderBrush="{DynamicResource MahApps.Brushes.AccentBase}"
                Background="{DynamicResource MahApps.Brushes.Control.Background}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border
                        Grid.Row="0"
                        BorderThickness="1"
                        BorderBrush="{DynamicResource MahApps.Brushes.AccentBase}"
                        Background="{DynamicResource MahApps.Brushes.Accent2}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                VerticalAlignment="Center"
                                TextAlignment="Center"
                                Text="{Binding CurrentMessage.Title}"/>
                            <Border
                                Grid.Column="1"
                                Margin="5,2">
                                <Button
                                    Style="{StaticResource MahApps.Styles.Button.Circle}"
                                    Command="{Binding ClearCurrentMessageCommand}">
                                    <iconPacks:PackIconMaterial Kind="CloseCircleOutline" />
                                </Button>
                            </Border>
                        </Grid>
                    </Border>
                    <Border
                        Grid.Row="1"
                        BorderThickness="1"
                        BorderBrush="{DynamicResource MahApps.Brushes.AccentBase}"
                        Background="{DynamicResource MahApps.Brushes.Accent4}">
                        <TextBlock
                            Margin="20,0"
                            VerticalAlignment="Center"
                            Text="{Binding CurrentMessage.Message}"/>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
