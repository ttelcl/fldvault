<UserControl
    x:Class="KeyLoader.Main.ServerWidget.ServerWidgetView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:local="clr-namespace:KeyLoader.Main.ServerWidget"
    mc:Ignorable="d" 
    d:DataContext="{d:DesignInstance local:ServerWidgetViewModel, IsDesignTimeCreatable=False}"
    d:DesignHeight="40" d:DesignWidth="40">
    <Border
        BorderThickness="1"
        Width="36"
        Margin="2"
        ToolTip="{Binding ServerActiveTooltip}"
        ToolTipService.Placement="Left"
        Background="{DynamicResource MahApps.Brushes.Control.Background}"
        BorderBrush="{DynamicResource MahApps.Brushes.AccentBase}">
        <iconPacks:PackIconMaterial 
            Height="24" Width="24"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            Kind="{Binding ServerActiveIcon}"
            Foreground="{Binding ServerActiveColor}"/>
    </Border>
</UserControl>
